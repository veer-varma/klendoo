# landing

Placeholder — this directory exists as of Sprint 0 to hold the repo's shape;
the marketing site and `/transparency` page (static list of settlement
transaction hashes, pulling from `ClientInteraction`) are Sprint 1 scope. See
`Klendoo_Sprint_Plan.md` → "Sprint 1 — First feature launch".
