{
  "name": "@klendoo/landing",
  "version": "0.0.0",
  "private": true,
  "description": "Marketing site + /transparency page.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "type-check": "tsc -b",
    "test": "vitest run",
    "generate:transparency": "node dist/transparency/generate.js"
  },
  "dependencies": {
    "@klendoo/db": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
