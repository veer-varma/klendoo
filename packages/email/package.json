{
  "name": "@klendoo/email",
  "version": "0.0.0",
  "private": true,
  "description": "Postmark email client, shared across agent services. Extracted from followup-reminder-agent in Sprint 2 once negotiation-agent needed the same client — see git history.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "type-check": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
