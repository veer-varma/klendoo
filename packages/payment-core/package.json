{
  "name": "@klendoo/payment-core",
  "version": "0.0.0",
  "private": true,
  "description": "x402 resource-server helpers: exposes Klendoo agent actions as real @x402/express-gated paid endpoints, settled by the GoPlausible facilitator, logged to ClientInteraction.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "type-check": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@klendoo/db": "*",
    "@x402/avm": "^2.21.0",
    "@x402/core": "^2.21.0",
    "@x402/express": "^2.21.0",
    "@x402/extensions": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
