{
  "name": "@klendoo/followup-reminder-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Agent 3 — Reminder & Follow-up. A real x402-gated GET /agents/reminder endpoint: caller pays via a custodial wallet (Intermezzo), payment-core logs the settlement, then the handler sends the reminder via Postmark.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "type-check": "tsc -b",
    "test": "vitest run",
    "start": "node dist/server.js",
    "trigger": "node dist/cli.js"
  },
  "dependencies": {
    "@klendoo/bazaar-listing": "*",
    "@klendoo/db": "*",
    "@klendoo/email": "*",
    "@klendoo/payment-core": "*",
    "@klendoo/trust-graph": "*",
    "@x402/avm": "^2.21.0",
    "@x402/express": "^2.21.0",
    "@x402/extensions": "^2.21.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
