{
  "name": "@klendoo/negotiation-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Agent 2 — Negotiation. A host proposes candidate times; invitees mark availability per slot via a poll link; a paid x402-gated endpoint activates and sends the poll. Deadline-triggered majority + calendar write is Sprint 3.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "type-check": "tsc -b",
    "test": "vitest run",
    "start": "node dist/server.js",
    "trigger": "node dist/cli.js"
  },
  "dependencies": {
    "@klendoo/db": "*",
    "@klendoo/email": "*",
    "@klendoo/payment-core": "*",
    "@x402/express": "^2.21.0",
    "@x402/extensions": "^2.21.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
